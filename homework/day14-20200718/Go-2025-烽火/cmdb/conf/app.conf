# 应用配置
AppName=CMDB
RunMode=dev

MaxMemory=104857600

# Web配置
AutoRender=true
ViewsPath=views
StaticDir=static

# 监控配置
[dev]
HTTPPort=9000
HTTPAddr=127.0.0.1

SessionOn=true
SessionName=sid
SessionProvider=file
SessionProviderConfig=tmp/session

include "mysql.conf"
include "auth.conf"