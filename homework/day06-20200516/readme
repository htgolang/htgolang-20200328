1. 知识整理
2. task数据持久化
    a. 用户可以选择持久化方式, gob,csv,json,自定义
        用户选择持久化数据 => 保存
        用户选择持久化方式 => 保存
    b. 增，删除，修改 => 保存
    c. 只保存task.csv(与持久化类型方式) 最近的N(3)次数据都进行持久化
        task.csv.0
        task.csv.1
        task.csv.2
        task.csv.3[删除0]
        task.csv.4[删除1]

3. tail命令 -f
    监控日志

4. 通信录/学生管理系统/学生成绩管理系统（随机做）
    用户登录
    通信录添加
    通信录编辑
    通信录查询
    通信录删除 (逻辑删除 数据不真删除，只通过某个属性标识是否被删除/真删除)
    密码修改
